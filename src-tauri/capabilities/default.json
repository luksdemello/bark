{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["tray-window"],
  "permissions": [
    "core:default",
    "opener:default",
    "fs:read-all"
  ]
}
